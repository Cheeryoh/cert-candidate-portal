# Worklog

This file tracks all meaningful work done on this project. AI is responsible for filling in all columns except **Human Approved**, which the human updates.

---

## How to Use

- AI creates a new row for each discrete task or feature.
- Entries are numbered sequentially.
- **Human Approved** is `Yes`, `No`, or `Pending` — human fills this in.
- If work is rejected or revised, AI adds a follow-up row referencing the original entry number.

---

## Log

| # | Date | Human Request | AI Suggestion | AI Actions Taken | AI Verification | Human Approved |
|---|------|---------------|---------------|------------------|-----------------|----------------|
| 1 | 2026-03-17 | Create project template repository with security protections, RULES.md, and WORKLOG.md | Create a minimal, stack-agnostic template with comprehensive .gitignore, CLAUDE.md for AI behavior, RULES.md for collaboration protocol, WORKLOG.md for logging, README scaffold, and Claude Code settings | Created `.gitignore`, `.gitattributes`, `RULES.md`, `WORKLOG.md`, `CLAUDE.md`, `README.md`, `.claude/settings.json` inside `project-template/` directory | Read each file after creation to confirm content; verified .gitignore covers .env, *.key, .vercel, .supabase; confirmed WORKLOG has all 5 required columns; confirmed CLAUDE.md references WORKLOG and RULES | Pending |
| 2 | 2026-03-17 | Implement cert-candidate-portal MVP from plan | Scaffold full Next.js 16 App Router portal with Supabase auth, portal pages, centralized query layer, Supabase migration + seed data, and Playwright e2e stubs | Pushed project-template to `Cheeryoh/project-template` (marked as GitHub template); created `Cheeryoh/cert-candidate-portal` from template; scaffolded Next.js 16 + Tailwind + shadcn/ui; added `@supabase/supabase-js` + `@supabase/ssr`; created `proxy.ts` (auth session middleware + route protection); created `lib/supabase/{client,server,queries}.ts`; created portal pages (dashboard, history, catalogue) and auth login page; created shadcn components (avatar, badge, card, table, separator, skeleton, sonner, dropdown-menu, navigation-menu); created `supabase/migrations/0001_initial.sql` (organizations, profiles, certifications, prerequisites, exam_attempts, eligibility view, RLS policies, auto-profile trigger); created `supabase/seed.sql` (4 orgs, 5 certs, 6 candidates, ~18 attempts covering all status types); created Playwright config + 4 e2e test stubs; committed and pushed | `npm run build` passes with 0 TypeScript errors; all 5 routes compile (/, /login, /dashboard, /history, /catalogue); proxy middleware registered correctly | Pending |
| 3 | 2026-03-17 | Apply Anthropic brand to cert-candidate-portal (dark palette, ANTHROP\C logo, Academy link) | Implement Anthropic dark theme via CSS variable overrides, new AnthropicLogo SVG component, branded login page with centered logo + Academy link top-right, branded sidebar with logo + Academy link | Created `components/brand/anthropic-logo.tsx` (SVG wordmark); rewrote `app/globals.css` (Anthropic dark palette — #141413 bg, #1c1c1a card, #e8ddd0 primary/oat, rgba borders — replaces light + dark blocks with single dark-only :root); updated `app/layout.tsx` (Geist → Inter, title → "Anthropic Certification Portal"); rewrote `app/(auth)/login/page.tsx` (dark layout, centered logo, Academy link top-right, dark card + inputs, oat CTA button); rewrote `components/nav/sidebar.tsx` (dark bg, logo mark, active item oat left-border, Academy link above Sign out) | `npm run build` passes with 0 TypeScript errors; all 6 routes compile cleanly | Pending |
| 4 | 2026-03-17 | Fix ANTHROP\C logo clipping — "A" and "C" cut off in SVG text rendering | Replace SVG `<text>` wordmark with plain HTML `<span>` (font-weight 700, letter-spacing 0.18em) — scales naturally, never clips | Rewrote `components/brand/anthropic-logo.tsx` to render a styled `<span>` instead of SVG | Playwright screenshot of `/login` confirms full "ANTHROP\C" wordmark visible, centered, no clipping | Pending |
| 6 | 2026-03-17 | Fix portal pages black-on-black text — hardcoded zinc/white classes unaffected by dark theme CSS variables | Replace all hardcoded `text-zinc-900`, `bg-white`, `bg-zinc-50`, `border-zinc-200` classes across portal pages and components with CSS variable-based Tailwind tokens (`text-foreground`, `bg-card`, `bg-background`, `border-border`, `text-muted-foreground`) | Updated `app/(portal)/layout.tsx`, `dashboard/page.tsx`, `history/page.tsx`, `catalogue/page.tsx`, `profile-card.tsx`, `stats-bar.tsx`, `attempts-table.tsx`, `cert-card.tsx` | Playwright screenshots of all 3 portal pages confirm full dark theme — text visible, cards readable, table correct | Pending |
| 5 | 2026-03-17 | Supabase setup: middleware, view RLS, auth test selector fix, seed script, full test run | (1) middleware.ts not needed — Next.js 16 uses proxy.ts natively, already active. (2) Add security_invoker migration for eligibility view. (3) Fix auth.spec.ts heading selector broken by login page rebrand. (4) Create idempotent seed script via Admin API. (5) Fix trigger blocking user creation via 0003 migration. (6) Fix two dashboard test strict-mode selector violations. | Confirmed proxy.ts active; created `supabase/migrations/0002_view_security.sql` + `0003_fix_trigger.sql`; fixed `e2e/auth.spec.ts` + `e2e/dashboard.spec.ts` selectors; created `scripts/seed.ts`; added tsx/dotenv devDeps + seed npm script; seeded 6 auth users + 12 exam attempts | All 18 Playwright tests pass (18/18, 21.8s); seed script idempotent and confirmed | Pending |
| 8 | 2026-03-17 | Security hardening P0–P3: rate limiting, security headers, deny-by-default routing, RLS eligibility trigger, error boundaries, input validation, seed credential cleanup | (P0-1) DB-backed rate limiter — 5 attempts / 15 min window / 15 min lockout; (P0-2) Security headers in next.config.ts — CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy, poweredByHeader:false; (P1-1) Proxy deny-by-default with public allowlist + trailing-slash-safe matching; (P2-1/P2-3) Seed script requires SEED_PASSWORD env var, removed hardcoded password + UUID logging; (P2-2) Normalised auth errors — single generic message regardless of failure reason; (P3-1) try/catch + PageError component on all 3 portal pages; redirect('/login') instead of return null; (P3-2) Server-side input validation in loginAction (email length/format, password length); (P3-3) DB trigger enforce_attempt_eligibility blocks ineligible inserts at DB layer | Created: migrations/0004_rate_limiting.sql, migrations/0005_eligibility_trigger.sql, lib/rate-limit.ts, app/(auth)/login/actions.ts, components/ui/page-error.tsx; Modified: next.config.ts, proxy.ts, login/page.tsx, dashboard/page.tsx, history/page.tsx, catalogue/page.tsx, scripts/seed.ts, .env.example, playwright.config.ts (headless gated on CI, loads .env.local), e2e/*.spec.ts (removed hardcoded password fallback) | TypeScript: 0 errors; grep audit: SeedPass123! removed from all source files; proxy deny-by-default logic manually traced | Pending |
| 7 | 2026-03-17 | Implement light/dark theme toggle — light mode as default, toggle in sidebar between Academy link and Sign out, persistent via localStorage | Wire `next-themes` ThemeProvider (already installed), define light `:root` palette and move dark values to `.dark {}` block, purge all hardcoded inline colour values from sidebar + login, create `ThemeToggle` component | Updated `globals.css` (light `:root` + `.dark {}` block); updated `layout.tsx` (ThemeProvider wrapping children, suppressHydrationWarning on html); created `components/ui/theme-toggle.tsx` (Sun/Moon toggle using useTheme); rewrote `components/nav/sidebar.tsx` (removed all inline styles + JS hover handlers, CSS variable tokens, added ThemeToggle between Academy link and Sign out); rewrote `app/(auth)/login/page.tsx` (removed all inline styles, CSS variable tokens, Tailwind focus ring); fixed residual `text-zinc-`/`bg-zinc-` in `attempts-table.tsx`, `cert-card.tsx`, `catalogue/page.tsx` | Grep audit confirms 0 hardcoded rgba/hex/zinc classes in all .tsx files; TypeScript type-check passes with 0 errors | Pending |

